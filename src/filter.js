//this state for filter

import { useState } from "react";

const[value,setvalue]= useState("all");

//filter logic

let filterDate = globalArr.filter((data)=>{
    let data = new Date(data.date)
    if(date.getFullYear){
        
    }
})